import {
    startUrl,
  } from './access';

const urls = {
    platform: {
        signIn: `${startUrl}/sign-in`,
        dashboard: `${startUrl}/dashboard`,
        client: `${startUrl}/clients/`,
    }
};

export default urls;