export default class ExpandedClientData {
    constructor({
        primaryRange,
        comparativeRange,
        total
    }) {
        this.primaryRange = primaryRange,
        this.comparativeRange = comparativeRange,
        this.total = total
    }
}